# Installation

## Requirements

| Requirement | Version | Purpose |
|------------|---------|---------|
| Python | 3.12+ | Runtime |
| Docker | 24+ | Sandbox execution, local infrastructure |
| Dapr CLI | 1.14+ | Agent sidecar |

## Install Nexus

=== "pip"

    ```bash
    pip install nexus-ai
    ```

=== "uv"

    ```bash
    uv add nexus-ai
    ```

=== "uv (dev project)"

    ```bash
    uv init my-agent
    cd my-agent
    uv add nexus-ai
    ```

### Optional extras

```bash
# Anthropic Claude support
pip install "nexus-ai[anthropic]"

# OpenAI GPT support
pip install "nexus-ai[openai]"

# Both providers
pip install "nexus-ai[all]"
```

### Verify the install

```bash
nexus --version
# nexus 0.1.0
```

---

## Install the Dapr CLI

Dapr provides the infrastructure runtime (state, pub/sub, workflows). Nexus requires the Dapr sidecar to be running alongside your agent.

=== "macOS"

    ```bash
    brew install dapr/tap/dapr-cli
    ```

=== "Linux"

    ```bash
    wget -q https://raw.githubusercontent.com/dapr/cli/master/install/install.sh -O - | /bin/bash
    ```

=== "Windows (PowerShell)"

    ```powershell
    powershell -Command "iwr -useb https://raw.githubusercontent.com/dapr/cli/master/install/install.ps1 | iex"
    ```

Initialize Dapr (installs the runtime components locally):

```bash
dapr init
```

Verify:

```bash
dapr --version
# CLI version: 1.14.x
# Runtime version: 1.14.x
```

---

## Start local infrastructure

Nexus requires PostgreSQL (with pgvector) and Redis. Use the provided `docker-compose.yml`:

```bash
# From your project directory (generated by nexus init)
docker compose up -d

# Verify all services are healthy
docker compose ps
```

Expected output:

```
NAME                STATUS
nexus-postgres      Up (healthy)
nexus-redis         Up (healthy)
nexus-placement     Up
```

---

## Environment variables

Set your LLM provider API key:

```bash
# Anthropic
export NEXUS_MODEL__ANTHROPIC_API_KEY="sk-ant-..."

# OpenAI
export NEXUS_MODEL__OPENAI_API_KEY="sk-..."
```

!!! tip "Using a .env file"
    Nexus reads `.env` files automatically. Create `.env` in your project root and add your keys there — they will not be committed to git if `.env` is in `.gitignore`.

---

## Next steps

- **[Quickstart →](quickstart.md)** — Build and run your first agent in 5 minutes
- **[Configuration reference →](../reference/config.md)** — Full `NexusConfig` field reference