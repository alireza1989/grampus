global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: nexus
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000   # adjust if Nexus runs elsewhere
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
