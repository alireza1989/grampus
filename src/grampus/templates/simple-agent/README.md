# {{project_name}}

A minimal Grampus agent using **{{model}}**.

## Usage

```bash
grampus run agent.py "What is the current price of gold?"
```

## Configuration

Edit `agent.py` to:
- Change the system prompt
- Add more tools
- Enable memory (`memory_enabled=True`)
- Adjust `max_iterations` and `temperature`
